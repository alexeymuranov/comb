%ol
  - @talks.each do |talk|
    %li{:id => "talk_#{ talk.id }"}
      - if talk.id == @form_talk_id
        = haml :'pages/org/_talk_form',
               :locals => { :talk => talk }
      - else
        = haml :'pages/org/_talk',
               :locals => { :talk => talk }
